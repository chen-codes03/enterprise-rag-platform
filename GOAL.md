# 项目目标定义：企业知识库 RAG 智能问答平台

## 1. 一句话目标
构建一个基于 RAG 的企业知识库智能问答平台，让 AI 能"读懂"企业私有文档（PDF/Word/Markdown）并准确回答业务问题，支撑智能客服与知识助手场景。

## 2. 背景与问题
- **传统文档检索效率低**：关键词检索无法理解语义，命中率和准确率差。
- **新员工培训成本高**：知识分散在多处，难以快速找到权威答案。
- **知识分散难复用**：缺乏统一的知识管理与 AI 问答入口，重复回答相同问题。

通过 RAG 检索增强生成，将企业私有知识注入大模型上下文，实现准确、可溯源的智能问答。

## 3. 核心目标（Must-Have）
- [ ] G1: 封装统一模型调用层，集成 DeepSeek、通义千问等大模型 API，支持同步/异步、SSE 流式输出，降低模型切换成本。
- [ ] G2: 完成 RAG 全链路：文档解析（PDF/Word/Markdown）→ 文本分块 → Embedding 向量化 → Chroma 向量库存储 → 语义检索 → 上下文拼接 → LLM 生成回答。
- [ ] G3: 设计 Embedding 与检索策略，基于 Chroma 实现语义检索，通过调整 Chunk 大小与重叠度优化召回效果，问答准确率提升约 35%。
- [ ] G4: 使用 Redis 缓存高频问答结果与 Embedding 向量，设计合理缓存失效策略，核心接口响应速度提升约 60%，降低大模型调用成本。
- [ ] G5: 基于 FastAPI 开发 RESTful API，完成 SSE 流式对话、请求鉴权（API Key）、参数校验；Docker 容器化一键启动。

## 4. 次要目标（Should-Have）
- S1: 搭建 AI 应用效果评估体系，通过问答准确率、召回率、响应延迟等指标持续优化 Prompt 与检索策略。
- S2: 支持多知识库管理（创建/删除/文档上传）。
- S3: 提供检索结果溯源（引用原文片段与来源文档）。

## 5. 非目标（Not-Now）
- N1: 不做前端 Web UI（本项目聚焦后端 API 与 RAG 链路，前端留接口）。
- N2: 不做用户体系与多租户权限管理（仅做 API Key 简单鉴权）。
- N3: 不做分布式/高可用集群部署（单机 Docker Compose 即可）。
- N4: 不做模型微调（仅用 RAG + Prompt 工程）。

## 6. 成功标准（可验证）
| 指标 | 目标值 | 验证方式 |
|------|--------|----------|
| RAG 链路可用 | 端到端问答成功 | 上传文档 → 提问 → 返回带溯源的回答 |
| 问答准确率 | 较纯关键词检索提升 ~35% | 评估集对比测试（基线 vs RAG） |
| 核心接口响应（缓存命中） | 提升 ~60% | 压测脚本对比有/无缓存 P95 延迟 |
| 流式输出 | SSE 正常逐 token 返回 | curl 调用 /chat/stream 验证 |
| 模型切换 | 改配置即可切换 DeepSeek/Qwen | 单测 + 切换后问答正常 |
| 部署 | `docker compose up` 一键启动 | 容器内健康检查通过 |
| 测试覆盖 | 核心模块单测覆盖率 ≥ 70% | pytest + coverage 报告 |

## 7. 技术约束
- **语言/框架**: Python 3.11 + FastAPI + LangChain
- **向量库**: Chroma（持久化本地存储）
- **缓存**: Redis
- **大模型**: DeepSeek、通义千问（通过统一模型层抽象）
- **文档解析**: PDF / Word / Markdown
- **部署**: Docker + Docker Compose
- **测试**: pytest + pytest-asyncio
- **配置管理**: pydantic-settings + .env

## 8. 里程碑
- **M1 - 模型层与基础设施**: 统一模型调用层（同步/异步/SSE）可工作，项目骨架 + 测试框架就绪。
- **M2 - 文档处理与向量库**: 文档解析 → 分块 → Embedding → Chroma 存储全链路打通。
- **M3 - RAG 问答链路**: 语义检索 → 上下文拼接 → LLM 生成，端到端问答可用。
- **M4 - 缓存与性能**: Redis 缓存问答结果与 Embedding，性能指标达标。
- **M5 - API 与鉴权**: FastAPI RESTful + SSE 流式 + API Key 鉴权 + 参数校验。
- **M6 - 评估与部署**: 评估体系脚本 + Docker Compose 一键部署。
